import React from "react";
import { motion } from "framer-motion";

const App = () => {
  return (
    <div>
      {/* <motion.div animate={{ scale: 0.5 }} />{" "} */}
      hello
    </div>
  );
};

export default App;
